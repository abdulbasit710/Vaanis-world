import {useEffect} from 'react';
const origin='https://www.vaanisworld.com';
type Props={title:string;description:string;path:string;type?:'website'|'book';schema?:Record<string,unknown>|Record<string,unknown>[]};
function setMeta(selector:string,attributes:Record<string,string>){let node:HTMLMetaElement|null=null;try{node=document.head.querySelector<HTMLMetaElement>(selector)}catch{node=Array.from(document.head.querySelectorAll<HTMLMetaElement>('meta')).find(item=>Object.entries(attributes).some(([key,value])=>key!=='content'&&item.getAttribute(key)===value))??null}if(!node){node=document.createElement('meta');document.head.appendChild(node)}Object.entries(attributes).forEach(([key,value])=>node!.setAttribute(key,value))}
export function Seo({title,description,path,type='website',schema}:Props){useEffect(()=>{const url=origin+path;document.title=title;setMeta('meta[name=description]',{name:'description',content:description});setMeta('meta[name=robots]',{name:'robots',content:'index, follow, max-image-preview:large'});[['og:title',title],['og:description',description],['og:type',type],['og:url',url],['og:image',origin+'/assets/soul-of-the-forest-front-cover.png']].forEach(([property,content])=>setMeta('meta[property='+property+']',{property,content}));[['twitter:card','summary_large_image'],['twitter:title',title],['twitter:description',description]].forEach(([name,content])=>setMeta('meta[name='+name+']',{name,content}));let canonical=document.head.querySelector<HTMLLinkElement>('link[rel=canonical]');if(!canonical){canonical=document.createElement('link');canonical.rel='canonical';document.head.appendChild(canonical)}canonical.href=url;document.getElementById('page-schema')?.remove();if(schema){const script=document.createElement('script');script.id='page-schema';script.type='application/ld+json';script.text=JSON.stringify(schema);document.head.appendChild(script)}return()=>document.getElementById('page-schema')?.remove()},[title,description,path,type,schema]);return null}
